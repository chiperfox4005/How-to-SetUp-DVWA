1. Update sistem & install paket dasar
   sudo apt update && sudo apt upgrade -y
sudo apt install -y apache2 mariadb-server php php-mysqli php-gd php-pdo php-cli php-mbstring git unzip composer libapache2-mod-php
